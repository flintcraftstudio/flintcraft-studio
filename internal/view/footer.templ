package view

import "fmt"

templ Footer() {
	<footer class="bg-fc-obsidian">
		<div class="mx-auto max-w-content px-6 py-16 lg:px-8">
			<!-- Top row: brand + nav -->
			<div class="flex flex-col justify-between gap-10 sm:flex-row sm:items-start">
				<!-- Brand -->
				<div>
					<a href="/" class="flex items-center gap-3">
						<img src="/static/images/logo.svg" alt={ SiteName } class="h-8 w-auto"/>
						<div>
							<div class="text-body-sm font-medium uppercase tracking-[0.12em] text-fc-parchment">
								{ SiteName }
							</div>
							<div class="font-display text-eyebrow italic tracking-[0.04em] text-fc-smoke">
								{ Tagline }
							</div>
						</div>
					</a>
				</div>
				<!-- Links -->
				<div class="flex flex-wrap gap-x-8 gap-y-3 text-ui uppercase">
					@footerLink("Services", "/services")
					@footerLink("Work", "/work")
					@footerLink("Process", "/process")
					@footerLink("About", "/about")
					@footerLink("Start a project", "/contact")
				</div>
			</div>
			<!-- Contact -->
			<div class="mt-10 border-t border-fc-flint pt-8">
				<div class="flex flex-col gap-4 text-body-sm tracking-normal text-fc-smoke sm:flex-row sm:items-center sm:gap-8">
					<span>{ Phone }</span>
					<span>{ Email }</span>
					<span>Helena, Montana</span>
					<a href={ templ.SafeURL(ReviewURL) } target="_blank" rel="noopener noreferrer" class="transition hover:text-fc-parchment">Leave a review</a>
				</div>
			</div>
			<!-- Copyright -->
			<div class="mt-8 flex flex-col gap-2 text-eyebrow tracking-normal text-fc-smoke sm:flex-row sm:items-center sm:justify-between">
				<p>&copy; { fmt.Sprint(Year()) } { SiteName }. All rights reserved.</p>
				<div class="flex gap-4">
					<a href="/privacy" class="transition hover:text-fc-parchment">Privacy</a>
					<a href="/terms" class="transition hover:text-fc-parchment">Terms</a>
				</div>
			</div>
		</div>
	</footer>
}

templ footerLink(label, href string) {
	<a
		href={ templ.SafeURL(href) }
		class="text-fc-smoke transition hover:text-fc-parchment"
	>
		{ label }
	</a>
}
